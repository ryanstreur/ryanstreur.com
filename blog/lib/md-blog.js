import fs from 'fs';

// Want to create a set of routes which mimic the directory structure

/**
 * Given a root directory with Markdown files inside, create a set of routes for each markdown file.
 * @param {string} folder 
 */
export function createRoutes(folder) {

}
