[project]
name = "server"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "daphne>=4.2.2",
    "django>=6.0.2",
    "twisted[http2,tls]>=26.4.0",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
]
